spring.config.import=optional:file:.env[.properties]
spring.cloud.config.uri=${CONFIG_SERVER_URL:http://localhost:8888}
spring.application.name=skt-ms-discovery
spring.profiles.active=${SPRING_PROFILE:local}
